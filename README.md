# customer-support-email-agent
AI Customer Support Email Agent — An intelligent email automation system that reads incoming emails, classifies requests, retrieves knowledge from a vector database, generates contextual responses using LLMs, and routes complex cases to human agents. Built with LangGraph state machines, FastAPI, and Retrieval-Augmented Generation (RAG).
